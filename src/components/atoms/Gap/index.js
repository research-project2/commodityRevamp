// @ts-check
import React from 'react';
import {View} from 'react-native';

/** @param {{width?: any, height?: any}} Params */
const Gap = ({width, height}) => {
  return <View style={{width: width, height: height}} />;
};

export default Gap;
